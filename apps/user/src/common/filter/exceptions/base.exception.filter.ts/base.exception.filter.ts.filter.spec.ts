import { BaseExceptionFilterTsFilter } from './base.exception.filter.ts.filter';

describe('BaseExceptionFilterTsFilter', () => {
  it('should be defined', () => {
    expect(new BaseExceptionFilterTsFilter()).toBeDefined();
  });
});
